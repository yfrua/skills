---
"mattpocock-skills": patch
---

`grilling` asks each frontier question through the harness's `question` tool when one is available, instead of always writing prose. Options carry the recommended answer marked and the free-text field covers anything not listed. The numbered `❓ … ➡️` text shape stays as the fallback when no question tool exists. Re-synced `docs/productivity/grilling.md` to match.